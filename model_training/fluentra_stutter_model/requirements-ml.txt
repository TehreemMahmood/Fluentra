# Heavy ML dependencies for the local stutter classifier.
# Kept separate from the Django app's requirements.txt on purpose:
# the live app stays API-only and lightweight; install these only where you
# actually want to run the trained model.
numpy
librosa
soundfile
scikit-learn
joblib
# wav2vec2 (default v2 model) needs torch + transformers
torch
transformers
# only needed for the YAMNet feature mode / the legacy 6-class model
tensorflow
tensorflow-hub
keras
